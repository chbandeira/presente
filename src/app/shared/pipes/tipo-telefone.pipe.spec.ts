import { TipoTelefonePipe } from './tipo-telefone.pipe';

describe('TipoTelefonePipe', () => {
  it('create an instance', () => {
    const pipe = new TipoTelefonePipe();
    expect(pipe).toBeTruthy();
  });
});
